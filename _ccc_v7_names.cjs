// Second pass: what does the card itself expose to AT? role + accessible name.
// If the card is a name-from-contents widget, the whole card text collapses into one label.
const base = require("./_ccc_v7_roles_lib.cjs");
