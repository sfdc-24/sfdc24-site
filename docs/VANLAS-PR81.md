# PR 81 overnight polish — status (ET)

**Tip:** restored `index.html` with humanized check note (`Looks clear — nothing here claims to have read a live customer system`) + `cli("looks clear — …")`.

**Already on branch (runtime):**
- `assets/overnight-polish-boot.js` — check label wrap, `engageLiveReply`, estimator one-pass
- `assets/chrome-footer-polish.js` — Method/Panels/Projects/Privacy/Terms + Governor/Intake/X-ray
- `assets/local-first-boot.js` loads both
- privacy/terms/governor footers; site-manifest aligned

**Peers (logged in metrics):** Claude/Gemini/Copilot NO — do not wait; Foundry YES ETA 120m; Claude wire-in = WIRE-FLEET (separate).

**Optional VANLAS follow-up (not blocking if runtime polish holds):**
- `assets/chrome.js` ensureFooter rebuild (Projects + trailing slashes baked in)
- `assets/triage.js`/`triage.py` clarify answer
- remaining static page footers + `data/agent-reply-metrics.jsonl`

Do not message the user; report PR URL + merge status to parent.
